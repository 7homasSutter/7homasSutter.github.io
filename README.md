# Home Page based on al-folio

## Get it started

```
bundle install
docker compose build
docker compose up
```

```
npx prettier --check .
# Auto-Fix the prettiere issues with this command:
npx prettier --write .
```
